<!DOCTYPE html>
<html>
  <head>
    <title>InterviewMGT</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="d-flex flex-column vh-100">
      <% if content_for?(:body_attributes) %>
          <%= yield(:body_attributes) %>
      <% end %>
      <header class="shadow-sm">
        <nav class="navbar navbar-expand-lg navbar-light bg-dark">
        <div class="container">
          <a class="navbar-brand p-3" href="/"><%= image_tag 'InterviewMGT.png',size: '150x50' %></a>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">

          <% if user_signed_in? %>
              <%= render 'layouts/header_users' %>
          <% elsif admin_signed_in? %>
            <%= render 'layouts/header_admins' %>
          <% else %>
            <%= render 'layouts/header_logout' %>
          <% end %>
            </ul>
        </div>
      </header>
      <main class="mb-auto">
    <%= yield %>
    </main>
    <footer class="bg-light text-center">
      <div class="container">
          <p class="my-0 text-dark py-3">Portfolio</p>
      </div>
  </footer>
  </body>
</html>
